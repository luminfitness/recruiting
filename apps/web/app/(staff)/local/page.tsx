export default function LocalQueuePage() {
  return (
    <div style={{ padding: "34px 40px" }}>
      <div style={{ fontSize: 11, letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--usapt-brand-blue)" }}>
        Field · Local manager
      </div>
      <h1 style={{ fontSize: 28, margin: "4px 0 0" }}>Trainer queue</h1>
      <p style={{ marginTop: 16, color: "var(--usapt-text-muted)", maxWidth: 560 }}>
        Ships in Phase 5 — the market-scoped referral queue with one-tap hired/declined/no-show outcome entry.
      </p>
    </div>
  );
}
