export default function RosterPage() {
  return (
    <div style={{ padding: "34px 40px" }}>
      <div style={{ fontSize: 11, letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--usapt-brand-blue)" }}>
        Live session
      </div>
      <h1 style={{ fontSize: 28, margin: "4px 0 0" }}>Interview roster</h1>
      <p style={{ marginTop: 16, color: "var(--usapt-text-muted)", maxWidth: 560 }}>
        Ships in Phase 2 — session booking, the token-redirect attendance handler, and the unmatched-participant
        confirm flow.
      </p>
    </div>
  );
}
