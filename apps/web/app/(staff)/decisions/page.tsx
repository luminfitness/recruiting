export default function DecisionsPage() {
  return (
    <div style={{ padding: "34px 40px" }}>
      <div style={{ fontSize: 11, letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--usapt-brand-blue)" }}>
        Decision queue
      </div>
      <h1 style={{ fontSize: 30, margin: "4px 0 0" }}>Decision bundle</h1>
      <p style={{ marginTop: 16, color: "var(--usapt-text-muted)", maxWidth: 560 }}>
        Ships in Phase 4 — the complete grade + quiz + disclosure bundle view with the four-way disposition.
      </p>
    </div>
  );
}
